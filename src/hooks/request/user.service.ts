import { ILogin } from "@/src/interfaces/auth";
import request from "@/src/services/api.request";

export const useUserRequest = () => {

  // const getMe = async (data: ILogin) => {
  //   return request.post('/auth/login', data);
  // }
  // return { getMe }
}
